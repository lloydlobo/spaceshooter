use crate::prelude::*;

//----------------------------------------------------------------

#[derive(Default, Component)]
pub struct Motion {
    pub velocity: Vec2,
    pub translation: Vec3,
}

//----------------------------------------------------------------
